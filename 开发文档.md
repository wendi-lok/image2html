# Image2Html 开发文档

## 项目简介
基于速创API的GPT-Image-2图片生成Web应用，支持本地上传参考图（自动传图床换取公网直链）、中英文切换、历史记录管理。

## 技术栈
- **后端**: Python 标准库 `http.server`（无需额外依赖）
- **前端**: HTML + CSS + JavaScript（单文件内联）
- **存储**: 本地JSON文件
- **图床**: 多图床适配器链，自动降级（默认 UAPI，免注册可用）

## 项目结构
```
Image2Html/
├── app.py              # Python后端主应用（含图床适配器链）
├── run.bat             # Windows一键启动脚本（需要 Python）
├── build_exe.bat       # 打包成免 Python 的 EXE + 分发 zip
├── requirements.txt    # Python依赖（无第三方依赖）
├── templates/
│   └── index.html      # 前端单页应用（含CSS/JS）
├── uploads/            # 上传的参考图（运行时自动创建，已 gitignore）
├── outputs/            # 生成的图片缓存
├── data/
│   ├── config.json     # API Key + 图床配置
│   └── history.json    # 历史记录
├── UserDoing.py        # API调用参考实现
├── docs/tutorial/      # 教程 PDF 的模板/构建脚本/截图（不参与运行）
├── Image2Html-使用教程.pdf  # 面向使用者的教程（12 页）
├── 设计要求.md
├── 开发日志.md
└── 开发文档.md
```

## 快速开始

### 方式一：一键启动（Windows，需要 Python）
双击项目根目录下的 `run.bat`。它会自动找到可用的 Python、启动服务、并用默认浏览器打开页面。
不需要 pip 安装（本项目零第三方依赖）。**关闭窗口即停止服务。**

### 方式二：手动启动
```bash
python app.py
```
浏览器会自动打开 `http://127.0.0.1:5000`，如未打开请手动访问。
端口被占用会自动顺延到 5001、5002…（终端会提示实际端口）。

### 方式三：免 Python（下载 Release / 自行打包）
从 Releases 下载 `Image2Html.zip`，解压双击 `Image2Html.exe` 即可；或双击 `build_exe.bat` 自行打包（见文末「EXE打包」）。

### 4. 设置API Key
点击右上角齿轮图标 ⚙，输入速创API密钥并保存。同一个弹窗里还可以选择参考图图床、填图床 Token。

## API接口文档

### 获取配置
```
GET /api/config
```
返回：
```json
{
  "api_key": "xxx",
  "masked": "xxxx****xxxx",
  "image_host": "auto",
  "host_uid": "",
  "host_token_masked": "",
  "host_saved": true,
  "host_options": [{"key":"auto","name":"自动选择（推荐）","need":"none","tip":"..."}]
}
```

### 保存配置
```
POST /api/config
Body: {"api_key": "your_key", "image_host": "auto", "host_token": "", "host_uid": ""}
```
- `api_key` 出现且为空 → 仍然返回 400（保持原有校验）
- 只想改图床、不改密钥时，`api_key` 字段可以整个不传
- 只想改密钥时，图床三个字段可以不传（不会被覆盖）

### 上传图片
```
POST /api/upload
Body: multipart/form-data (field: file)
```
返回：
```json
{
  "url": "https://uapis.cn/static/uploads/xxx.webp",
  "filename": "xxx.png",
  "host": "uapis",
  "host_name": "UAPI",
  "public": true,
  "local_url": "http://127.0.0.1:5000/uploads/xxx.png"
}
```
图床全部失败时，`public` 为 `false`，`url` 回退成本地地址，并额外返回 `warning` 与 `errors`：

```json
{
  "url": "http://127.0.0.1:5000/uploads/xxx.png",
  "host": "local", "public": false,
  "warning": "图床均不可用，已回退为本地地址（速创服务器可能无法访问）",
  "errors": ["UAPI: ...", "Catbox: ..."]
}
```

**降级成功时也会返回 `warning`。** 这一点很重要：默认图床失败、换用备用图床后虽然 `public` 为 `true`，
但备用图床可能限时失效（如 Uguu 约 3 小时），不能让用户蒙在鼓里：

```json
{
  "url": "https://d.uguu.se/xxxx.png",
  "host": "uguu", "host_name": "Uguu", "public": true,
  "warning": "默认图床不可用，已自动换用「Uguu」。该图床链接仅保留约 3 小时，生成前若已过期会抓不到图",
  "errors": ["UAPI: 图片上传接口限流中，每日限制10次，请明天再试 ...", "Catbox: RemoteDisconnected: ..."]
}
```

前端由 `lastUploadWarning` 接住，在 `showRefWarning()` 里优先展示这条具体原因。

## 参考图图床（重要）

速创 API 的 `urls` 参数只认公网 URL，本地的 `http://127.0.0.1:5000/uploads/...` 它抓不到。
因此本地上传的参考图会先传到图床，拿到公网直链再提交。

**适配器链**（`app.py` 顶部 `IMAGE_HOSTS` / `AUTO_ORDER`），按顺序尝试，第一个成功就用：

| 顺序 | key | 名称 | 需要凭据 | 说明 |
|------|-----|------|----------|------|
| 1 | `uapis` | UAPI | 免注册 | `uapis.cn`，**默认可用**；匿名每日 10 张，填免费 Key 后不限量 |
| 2 | `picui` | PicUI | token | 国内图床，Token 在「个人中心」生成（`Bearer`） |
| 3 | `smms` | SM.MS | token | 接口已迁移到 `s.ee`；**要求账号有有效套餐**，否则拿不到 Token |
| 4 | `imgurl` | ImgURL | uid + token | UID 与 Token **都放请求体** |
| 5 | `imgbb` | ImgBB | API Key | 海外 |
| 6 | `catbox` | Catbox | 免注册 | 海外 |
| 7 | `uguu` | Uguu | 免注册 | 海外，链接约 3 小时失效 |
| 8 | `telegraph` | Telegraph | 免注册 | 海外，部分国内网络不可达 |
| — | `local` | 仅本地 | — | 不上传，调试用 |

- 设置 `image_host` 为某个具体 key 时**只试该图床**；设为 `auto` 时按上表顺序。
- `auto` 模式下，`need != none` 且没填凭据的图床会被 `host_ready()` **直接跳过**，不会白等超时。
- 想加新图床：在 `IMAGE_HOSTS` 加一项，在 `upload_to_host()` 里加一个分支，`AUTO_ORDER` 里排好位置即可。
- 添加新适配器时注意：`urllib` 对 `POST + 308/307` 不会自动跟随重定向（会抛 `HTTPError`），需要自己处理。

### 必须带浏览器 UA（否则 Cloudflare 拦）

`urllib` 默认 UA 是 `Python-urllib/3.x`。`s.ee`（SM.MS 的新域名）前面挂了 Cloudflare，
会把它当爬虫直接拒掉，**返回 403 且响应体不是 JSON**（`error code: 1010`），
前端只能看到一句没头没尾的失败——即使 Token 完全正确也一样。

所以 `http_request()` 在调用方没显式指定 `User-Agent` 时补一个常见的浏览器 UA：

```python
DEFAULT_UA = ("Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
              "(KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36")
...
if not any(k.lower() == 'user-agent' for k in headers):
    headers['User-Agent'] = DEFAULT_UA
```

对照实验：

| UA | `s.ee` 返回 |
|----|-------------|
| 默认 `Python-urllib/3.x` | `403` / `error code: 1010`（非 JSON） |
| 浏览器 UA | `401` / `{"code":-1,"message":"Unauthorized"}`（正常的鉴权错误） |

> 加新图床时如果遇到「403 且响应体不像 JSON」，先怀疑 UA，而不是接口挂了。

### `smms` 适配器为什么试 4 次

`sm.ms` 的上传接口已迁移到 `s.ee`，而两家文档在**成功码**和**鉴权写法**上都不一致：

| | 成功码 | 鉴权 |
|---|--------|------|
| `sm.ms` v2 | `code = 200` | 裸 token |
| `s.ee` | `code = 0` + `message` | 文档写 `Bearer` |

用错误的 token 去试时，两种写法的返回**一模一样**（都是 401 `Unauthorized`），无法据此区分，
因此 4 种「端点 × 鉴权」组合依次尝试，token 正确时第一次就命中：

```
s.ee + Bearer  →  s.ee + 裸 token  →  sm.ms + 裸 token  →  sm.ms + Bearer
```

两个判据上的讲究：

- **成功判据是「有没有拿到 `http` 开头的 URL」**，而不是猜状态码——各版本成功码不同，猜必然出错。
- **错误信息优先返回非 `HTTP ` 开头的**。否则前面拿到的 `Unauthorized`（真正有用的原因）
  会被后面 `sm.ms` 旧域名的裸 `HTTP 308` 跳转盖掉，用户看到的就是句废话。

重复上传时 `s.ee` 会返回 `code = 'image_repeated'` 并带上 `images`，这种情况直接复用已有 URL。

## 前端拖拽上传

| 交互 | 实现 |
|------|------|
| 窗口内任意位置拖入文件 | `document` 上的 `dragenter/dragover/dragleave/drop`，全屏遮罩 `#dropOverlay` |
| 拖到参考图区域 | `.ref-section` 同步高亮，`.ref-item` 的 drop 也接受文件 |
| Ctrl+V 粘贴截图 | `document` 的 `paste`，读 `clipboardData.items` |
| 点击「+」选文件 | 隐藏的 `#fileInput` |

三者都汇入同一个入口 `handleFiles()`，内部按 `pendingUploads` 计数控制 5 张上限。

**与排序拖拽的隔离**：排序拖拽只写 `text/plain`；文件拖拽由 `isFileDrag()` 判断
`dataTransfer.types` 是否含 `Files`。`.ref-item` 的 `dragover/drop` 对文件拖拽做 `stopPropagation`，
不会和排序互相误触发。

> 调试提示：`refImages`、`pendingUploads` 等在 `index.html` 里是用 `let` 声明的，
> **不会挂到 `window` 上**，在控制台/测试脚本里要用 `eval('refImages')` 或直接在页面脚本内访问。

## 国际化（中英切换）

静态文案靠 `[data-i18n]` 标注，`applyLang()` 统一替换。

**但 JS 动态生成的内容不在 `[data-i18n]` 覆盖范围内**，必须由 `applyLang()` 主动重渲染，
否则切到英文后这些块还是中文：

```javascript
function applyLang() {
  // ...替换 [data-i18n] 文案...
  renderRefGrid();      // 参考图卡片/占位符
  showRefWarning();     // 降级/回退警告条
  const sel = document.getElementById('hostSelect');
  if (sel && hostOptions.length) renderHostOptions(sel.value || 'auto');  // 图床下拉
}
```

> 加新的动态渲染内容时，记得同步往 `applyLang()` 里补一次调用。

## 启动与端口

| 环境变量 | 作用 |
|----------|------|
| `IMAGE2HTML_PORT` | 指定端口（默认 5000） |
| `IMAGE2HTML_NO_BROWSER=1` | 启动时不自动打开浏览器（调试用） |

- 浏览器一律用 `http://127.0.0.1:PORT` 打开，**不要用 `localhost`**：Windows 上 `localhost` 可能先解析到 `::1`，而服务只监听 IPv4，页面会打不开。
- 端口被占用时 `find_free_port()` 会自动往后找空闲端口。
- 重复启动时 `probe_port()` 会请求 `/api/config` 确认「是不是本程序」：是则只打开浏览器，不重复起服务；被别人占用则换端口。
- Windows 下 `AppServer.allow_reuse_address = False`。Windows 的 `SO_REUSEADDR` 允许两个进程绑同一端口，谁收到连接看运气，比直接失败更难排查。

`run.bat` 的探测逻辑（`where python` 可能命中微软商店占位符，所以必须实际执行一次验证）：

```bat
where python >nul 2>&1
if not errorlevel 1 (
    python -c "import sys" >nul 2>&1
    if not errorlevel 1 set "PYEXE=python"
)
if not defined PYEXE ( ...回退到 py -3... )
```

---

### 生成图片
```
POST /api/generate
Body: {"prompt": "...", "size": "auto", "urls": [...]}
```
返回：`{"success": true, "task_id": "xxx"}`

### 查询任务
```
GET /api/task/<task_id>
```
返回：`{"status": 2, "result_urls": [...], "message": ""}`
- status=1: 生成中
- status=2: 成功
- 其他: 失败

### 保存图片到本地
```
POST /api/save-image
Body: {"url": "https://..."}
```
返回：`{"success": true, "filename": "xxx.jpg", "local_url": "outputs/xxx.jpg"}`

### 历史记录
```
GET    /api/history           # 获取历史记录
POST   /api/history           # 添加记录
PUT    /api/history/<id>      # 更新记录
DELETE /api/history/<id>      # 删除记录
```

## 速创API参考

- **提交接口**: POST `https://api.wuyinkeji.com/api/async/image_gpt?key=xxx`
- **查询接口**: GET `https://api.wuyinkeji.com/api/async/detail?key=xxx&id=task_id`
- **请求头**: `Authorization: api_key`, `Content-Type: application/json`
- **请求体**:
  ```json
  {
    "prompt": "提示词",
    "size": "auto",
    "urls": ["参考图URL1", "参考图URL2"]
  }
  ```
- **size可选值**: auto, 1:1, 3:2, 2:3, 16:9, 9:16, 4:3, 3:4, 21:9, 9:21, 1:3, 3:1, 2:1, 1:2
- **参考图**: 最多5张，需公网可访问的URL
- **计费**: 0.1元/张

## 注意事项

1. **参考图URL**: 本地上传的图片会先传到图床换成公网直链。默认的 UAPI 免注册即可用（匿名每日 10 张）；
   若要更多额度或在设置里选定了别的图床，需要填写对应的 Token / API Key。
   如果所有图床都失败，会回退成本地地址，此时速创服务器通常抓不到，界面上会给出红色提示。
2. **生成时间**: 图片生成时间不定，前端会自动轮询查询结果。
3. **错误处理**: 所有错误都会在前端通过Toast提示显示；图床逐个失败的详细原因会打印在浏览器控制台（`[upload]`）。
4. **数据持久化**: API Key、图床配置和历史记录保存在 `data/` 目录下的JSON文件中。
5. **Uguu / Telegraph** 属于兜底图床：前者链接约 3 小时失效，后者部分国内网络不可达，界面上会给出提醒。

---

## 补充文档（后续迭代）

### 任务状态码说明（重要）

速创API的异步任务 `status` 字段取值：

| status | 含义 | 前端处理 |
|--------|------|----------|
| **0** | 排队中（已提交未开始生成） | 视为"生成中"，继续轮询 |
| **1** | 生成中 | 视为"生成中"，继续轮询 |
| **2** | 生成成功 | 展示 `result_urls` 结果图 |
| 其他 | 生成失败 | 报错并更新历史为 `failed` |

> **关键坑**：status 可能以字符串或数字返回。后端 `handle_query_task` 会 `int()` 转换，并把 `0` 归一化为 `1`（排队中=生成中）；前端 `pollTask` / `queryByTaskId` 用宽松相等 `==` 双重保险，且判断条件为 `status != 1 && status != 0` 才视为错误。

### 任务ID（task_id）功能

- 提交生成任务后，API 返回 `task_id`，用于后续查询结果。
- 前端在加载区域和结果区均展示可点击复制的 `task_id`。
- 历史记录通过存储的 `task_id` 实时向API查询最新结果，**不依赖**本地缓存的 `result_urls`（可能过期或为空）。

#### 前端新增函数
- `copyTaskId(taskId)` — 复制任务ID到剪贴板（含旧浏览器fallback）。
- `queryByTaskId(taskId)` — 通过 task_id 调用 `/api/task/<id>` 查询结果，处理 status 0/1/2。
- `loadHistory()` — 重写：历史卡片展示 task_id（等宽字体，点击复制）、🔍查询按钮（重新查询）、📷占位缩略图；点击整卡 = 按 task_id 查询。
- `displayResults(urls, taskId)` — 新增 taskId 参数，结果区显示可复制任务ID + 🔄重新生成按钮。

#### 历史记录行为
- 卡片点击 / 🔍按钮 → `queryByTaskId(task_id)` 实时查询。
- 已删除原 `loadHistoryResult` 函数（改为直接查询API）。

### 新增i18n翻译键（中英双语）
`task_id_label`、`query_task`、`querying`、`task_id_copied`、`no_result`、`still_generating`。

---

## EXE打包（PyInstaller）

### 一键打包
直接双击仓库根目录的 `build_exe.bat`。它会自动找 Python、装 PyInstaller、打包，并在 `dist\` 下同时产出免安装文件夹和可直接分发的 zip。

### 手工打包命令
```bash
pyinstaller --onedir --clean --noconfirm --name Image2Html ^
  --add-data "templates;templates" ^
  --exclude-module tkinter --exclude-module unittest --exclude-module pydoc ^
  --exclude-module test --exclude-module distutils --exclude-module lib2to3 ^
  app.py
```

> `--add-data` 在 Windows 上是 `源;目标`（分号），Linux/macOS 是 `源:目标`（冒号）。
> 写错不会报错，只是模板没打进去，运行时首页才 404。
> 另外**不能排除 `email`**：`http.server` 解析请求头依赖它。

### 输出结构（约 23MB，zip 后约 8.8MB）
```
dist/Image2Html/
├── Image2Html.exe       # 主程序（双击运行）
├── 启动.bat              # 备用启动脚本（出错时 pause 保留窗口，内容全 ASCII）
├── 使用说明.txt
└── _internal/           # Python运行时 + 模板，必须和 exe 放一起
    └── templates/
        └── index.html
dist/Image2Html.zip      # 分发包，发这个
```

### 路径处理逻辑（app.py）
```python
if getattr(sys, 'frozen', False):
    BUNDLE_DIR = sys._MEIPASS                                   # 只读资源（模板）
    USER_DIR = os.path.dirname(os.path.abspath(sys.executable)) # 用户数据（exe 同级）
else:
    BUNDLE_DIR = USER_DIR = os.path.dirname(os.path.abspath(__file__))
```

- **只读资源**（`templates/`）从 `sys._MEIPASS`（即 `_internal/`）读。
- **用户数据**（`uploads/`、`outputs/`、`data/`）落在 **exe 同级目录**，所以换电脑直接拷文件夹即可迁移配置。
  这里用 `sys.executable` 而不是 `sys.argv[0]`：某些启动方式下 `argv[0]` 是相对路径，`dirname` 会得到空串，数据目录就散到当前工作目录去了。
- `print` 带 `flush=True`，解决打包后控制台看不到输出（缓冲）的问题。

### 中文编码
- `启动.bat` **内容全 ASCII**，不出现中文——cmd 按当前代码页读 bat，和 `chcp 65001` 互相打架，很容易乱码。
- `使用说明.txt` 由 PowerShell 以 `-Encoding UTF8` 写出。
- 打 zip **不能用 `Compress-Archive`**（Windows PowerShell 5.1 不写 UTF-8 标志位，中文文件名解压后乱码），改用 `.NET` 的
  `ZipFile.CreateFromDirectory($src, $dst, 'Optimal', $true, [Text.Encoding]::UTF8)`。
  校验方法：条目的 `flag_bits & 0x800` 应为真。

### 分发方法
把 `dist\Image2Html.zip` 发给他人。接收方解压后双击 `Image2Html.exe`（或 `启动.bat`），浏览器自动打开 `http://127.0.0.1:5000`，首次需在设置里填 API 密钥。`uploads/`、`outputs/`、`data/` 首次运行自动创建在 exe 同级目录。

> **不要**把 `Image2Html.exe` 单独拖出来用——它依赖同级 `_internal/`，单独拷会起不来。

---

## 速创账号与计费（给使用者看的部分）

| 项 | 值 |
|----|-----|
| 免费额度 | **无**。没余额/点数就一定调用失败，这是最常见的「填了 Key 还报错」原因 |
| 单价 | 0.1 元/张（扣 10 点/张） |
| 计费优先级 | vip专属 → 单独包月 → 次数包 → 点数计费 → 账户余额 → 免费额度 |

控制台入口（同一个站点，登录后从右上角进）：

| 页面 | 地址 | 用途 |
|------|------|------|
| 注册 | `https://api.wuyinkeji.com/user/register` | 邮箱/手机号 + 6 位验证码；密码 ≥8 位且含字母和数字 |
| 登录 | `https://api.wuyinkeji.com/user/login` | 微信扫码 或 邮箱密码 |
| **订单管理** | `https://api.wuyinkeji.com/user/order` | **充值、查余额/点数** |
| 密钥管理 | `https://api.wuyinkeji.com/user/key` | 取要填进本应用的 API Key |
| 会员 | `https://api.wuyinkeji.com/user/vip` | 查看 vip/包月 |

## 使用教程 PDF

面向使用者的教程在仓库根目录：**`Image2Html-使用教程.pdf`**（A4，12 页）。
章节：解压启动 / 速创注册与充值 / 图床申请与使用 / 生成流程 / 排错 / 附录。

它的源码与截图在 `docs/tutorial/`，改了界面之后可以重新生成（只要 Python 标准库 + 本机有 Edge）：

```bash
python docs/tutorial/build_tutorial.py     # 产物直接覆盖仓库根目录的 PDF
```

细节见 `docs/tutorial/README.md`（含截图怎么拍的、以及 headless Edge 的三个坑）。

