# Image2Html 开发日志

## 2026-08-11

### 项目初始化
- 阅读设计要求.md，理解项目需求
- 研究速创API文档（GPT-Image-2接口）
- 分析UserDoing.py中的API调用逻辑
- 确认API端点和参数格式：
  - 提交任务：POST `https://api.wuyinkeji.com/api/async/image_gpt`
  - 查询结果：GET `https://api.wuyinkeji.com/api/async/detail`
  - 请求参数：prompt(必填), size(可选), urls(可选-参考图)

### 需求确认
- 无需用户认证，仅需输入API Key
- 需要历史记录功能
- 需要中英文切换支持
- 参考图最多5张，支持排序
- 生成图片可预览、保存本地、重新生成

### 技术方案
- 后端：Python Flask
- 前端：HTML + CSS + JS（单文件内联）
- 存储：本地JSON文件（config.json, history.json）
- 图片：本地文件系统（uploads/, outputs/）

### 实现完成
1. 创建项目目录结构
2. 编写 requirements.txt
3. 编写 app.py — Flask后端
   - API Key配置管理
   - 图片上传接口
   - 生成任务提交接口
   - 任务轮询查询接口
   - 图片保存接口
   - 历史记录CRUD接口
4. 编写 templates/index.html — 前端单页应用
   - 响应式布局（左右分栏）
   - 中英文切换（data-i18n + JS翻译字典）
   - 参考图上传（本地上传 + URL粘贴）
   - 参考图排序（拖拽 + 按钮）
   - 生成结果预览
   - 历史记录管理
   - 暗色模式支持（跟随系统）
5. 创建开发文档

### 反馈1修复 — 2026-08-11（第二轮）
根据设计要求.md中的反馈1部分，进行以下修复：

1. **程序入口优化**
   - app.py添加 `webbrowser` 自动打开浏览器功能
   - 创建 `run.bat` Windows一键启动脚本
   - 启动时自动检测Python环境、安装依赖

2. **中英文转换完善**
   - 补全zh中文翻译字典中所有缺失的翻译（15+条目）
   - 新增翻译键：regen、poll_status、elapsed_time
   - 更新 en 字典保持同步

3. **字体大小调整**
   - 全局字体增大2-3px：form-label 13→14px, form-input 14→15px, btn-primary 15→16px
   - btn-secondary 13→14px, result-placeholder 15→16px, toast 14→15px
   - logo 18→20px, history prompt-text 13→14px, meta 11→12px
   - alert-box 13→14px, modal-title 17→18px, history-empty 13→14px

4. **核心功能（轮询）修复**
   - 后端 handle_query_task 添加 status 类型转换（str→int），防止类型不匹配
   - 前端 pollTask 使用宽松相等（==）代替严格相等（===）
   - 添加连续错误计数（consecutiveErrors），超过5次才终止
   - 轮询过程中实时显示已查询次数和等待时间
   - 最大轮询次数从120增至180（6分钟超时）
   - 捕获异常时记录console.error，不再静默忽略
   - 失败时将历史记录状态更新为 'failed'
   - displayResults 添加保存图标和"重新生成"按钮
   - 生成结果区域失败时显示具体错误信息

### 修复 status=0 误报失败 — 2026-08-11（第三轮）
问题：API返回 `status: 0`（排队中）被误判为失败，提示"生成失败: Status code: 0"。

1. **后端 app.py `handle_query_task`**
   - 增加 status 0 归一化逻辑：`if status_code == 0: status_code = 1`
   - 明确三种状态：0=排队中(视为进行中)、1=生成中、2=成功、其他=失败

2. **前端 index.html `pollTask`**
   - 错误判断从 `data.status != 1` 改为 `data.status != 1 && data.status != 0`
   - 注释说明 status 各取值含义

### 任务ID功能 + 历史记录重构 — 2026-08-11（第四轮）
需求：历史记录模块原本只缓存本地 `result_urls`，实际不可用；需存储并显示API返回的 `task_id`，通过它查询生成结果。

1. **task_id 展示**
   - `generate()` 提交后，加载区域显示可复制/可选中复制的 task_id
   - `displayResults(urls, taskId)` 新增 taskId 参数，结果区展示可点击复制的任务ID + 🔄重新生成按钮
   - `pollTask` 成功时传入 taskId 给 displayResults

2. **新增函数**
   - `copyTaskId(taskId)`：复制到剪贴板，含旧浏览器 textarea fallback
   - `queryByTaskId(taskId)`：调 `/api/task/<id>` 实时查询，处理 status 0/1/2 三种情况

3. **历史记录重构（loadHistory重写）**
   - 每张卡片显示：📷缩略图（无结果时占位）、提示词、**任务ID（等宽字体，点击复制）**、时间/尺寸/状态
   - 🔍查询按钮 = 按 task_id 向API重新查询结果
   - 点击整卡 = 按 task_id 查询（替代原先仅显示本地缓存）
   - 删除无用的 `loadHistoryResult` 函数

4. **新增i18n翻译键**：task_id_label、query_task、querying、task_id_copied、no_result、still_generating（中英双语）

### EXE打包（PyInstaller）— 2026-08-11（第五轮）
需求：封装成exe，打包到单一文件夹，便于分发给他人使用。

1. **app.py 路径处理改造（支持PyInstaller）**
   - 引入 `sys`，通过 `sys.frozen` 检测打包环境
   - `BUNDLE_DIR = sys._MEIPASS`：模板等只读文件从 `_internal/` 读取
   - `USER_DIR` = exe同级目录：`uploads/`、`outputs/`、`data/` 用户数据持久化
   - `print` 添加 `flush=True` 解决打包后无控制台输出问题

2. **打包**
   - 安装 PyInstaller 6.17.0
   - 命令：`pyinstaller --onedir --name Image2Html --add-data "templates;templates" --noconfirm app.py`
   - 输出：`dist/Image2Html/`（约20MB），含 `Image2Html.exe`、`_internal/`（含模板）、`启动.bat`

3. **验证**
   - 用 subprocess 启动exe，HTTP请求 `http://localhost:5000/` 返回200、HTML正确
   - 验证模板已打包进 `_internal/templates/index.html`、`queryByTaskId`/`task_id_label` 均在
   - 注意：测试时需先清理残留的Image2Html进程（多个进程同时占用5000端口会冲突）

4. **排障记录**
   - Windows下 exe 进程若未完全终止，会残留占用5000端口导致新实例"timed out"；用 `cmd /c "taskkill /f /im Image2Html.exe /t"` 强制清理
   - PyInstaller 打包的 DLL 被占用时，`--clean` 重建会报 PermissionError；需先杀进程再删除 build/dist

---

## 2026-09-13（第六轮：图床拖拽上传 + 一键启动修复）

需求只有两点：**① 接一个免费稳定的图床 API，支持直接拖拽图片上传；② 修好一键启动入口。**
结构、UI、算法按要求不动。

### 一、图床接入

**先说结论：纯粹「免注册 + 国内可直连 + 有开放 API」的图床，2025 年后基本已经没有了。**
实测结果（失败原因都记录在案，避免以后重复踩）：

| 图床 | 实测结果 |
|------|----------|
| sm.ms | `308` 跳转到 `s.ee/api/v1/file/upload`，且必须带 token（`Unauthorized`） |
| ImgURL | `/api/v2/upload` 强制要求 `uid` + `token` |
| cdnjson.com | Chevereto 接口，`Invalid API key` |
| 0x0.st | 官方已停服（"uploads disabled"） |
| 路过图床 / 牛图网 / Hello图床 / Picbed / Z4A | 接口 404 或返回 HTML，无公开上传 API |
| freeimage.host / api.imgbb.com | 需要自备 API Key，且为海外站点 |
| catbox.moe / tmpfiles / telegraph | 部分返回空响应，telegraph 国内不可达 |
| **uapis.cn** | ✅ `POST /api/v1/image/upload`，field=`file`，**免注册直接返回直链** |

所以最终做成**适配器链**（`app.py` 的 `IMAGE_HOSTS` + `AUTO_ORDER`）：

```
uapis(免注册) → picui → smms(s.ee) → imgurl → imgbb → catbox → uguu → telegraph → 本地回退
```

- **免注册开箱可用**：默认 `auto` 模式下第一个就是 UAPI，拖进图片就出公网直链。
  UAPI 匿名访客每日 10 张；在设置里填上免费 API Key（`Authorization: Bearer <key>`）即不限量。
- **自动降级**：前一个失败就换下一个，全部失败才退回原来的本地 URL，并如实把每个图床的失败原因回传前端。
- **未配置凭据的图床会被直接跳过**（`host_ready()`），否则每次上传都要白等一圈超时。
- 本地上传依然会在 `uploads/` 留一份（原有行为保留，便于本地预览）。

新增代码：`encode_multipart()`（标准库没有 multipart 编码，手写）、`upload_to_host()`、`upload_reference_image()`、`host_ready()`。
`/api/upload` 响应扩展为 `{url, filename, host, host_name, public, local_url, warning?, errors?}`——**新增字段，原字段 `url` / `filename` 语义不变，向后兼容**。
`/api/config` 新增 `image_host` / `host_token` / `host_uid` 三项，并返回 `host_options` 供前端渲染下拉框；`POST` 时 `api_key` 为空仍然返回 400（原契约不变）。

### 二、前端拖拽上传

- **全局拖拽**：文件拖进窗口任意位置 → 全屏遮罩提示「松开鼠标即可上传参考图」，参考图区域同时高亮。
- **参考图区域拖拽**、**Ctrl+V 粘贴截图**，与原有「点击 + 选文件」共用同一个 `handleFiles()` 入口。
- **和排序拖拽做了隔离**：排序用的是 `text/plain`，文件拖拽靠 `isFileDrag()` 判断 `dataTransfer.types` 是否含 `Files`；`.ref-item` 的 `dragover/drop` 里对文件拖拽做了 `stopPropagation`，不会互相误触发。
- 上传中显示 spinner 占位，成功后替换为真实图片并打上**来源徽标**（如 `UAPI`；回退本地时标红 `local`）。
- 用 `pendingUploads` 计数，避免并发上传突破 5 张上限；生成时若仍在上传会拦截并提示。
- 设置弹窗新增「参考图图床」下拉 +「图床 Token / API Key」输入框（切到 ImgURL 时额外显示 UID 输入框），原有样式类复用，未改动整体 UI 结构。

### 三、一键启动修复

原 `run.bat` 有两个真实故障点：

1. **`where python` 会被 Windows「应用执行别名」骗到**——它找到的是微软商店的占位符，`where` 返回成功，但执行时只会弹出商店，脚本还继续往下跑。这是「一键启动点了没反应」的最可能原因。
   → 改为**实际执行 `python -c "import sys"` 验证**，失败再退回 `py -3`。
2. **`start "" python app.py`** 新开一个窗口跑服务，一旦 `app.py` 启动即崩，窗口一闪而过，用户什么都看不到。
   → 改为**在当前窗口前台运行**，报错直接显示，结尾 `pause` 保留窗口。
   另外去掉了 `pip install -r requirements.txt`（本项目零第三方依赖，这步既拖慢启动，无网络时还会卡住）。

`app.py` 启动链路也一并加固：

- **浏览器改用 `http://127.0.0.1:PORT` 打开**。原来用 `localhost`，Windows 上可能优先解析到 IPv6 的 `::1`，而服务只监听了 IPv4，浏览器就会打不开——这是另一个典型坑。
- **端口占用自动切换**：`find_free_port()` 从 5000 往后找空闲端口，不再直接崩。
- **重复启动不再报错**：`probe_port()` 会真的请求一次 `/api/config` 判断「是不是本程序」，是就只打开浏览器、不重复起服务；被别人占用则换端口。
  （只判断端口连接会把别的程序误判成自己，所以必须探接口。）
- **Windows 上关掉 `SO_REUSEADDR`**：Windows 允许两个进程绑同一端口，谁收到连接看运气，比直接失败更难查。`AppServer.allow_reuse_address = (os.name != 'nt')`。
- 新增环境变量 `IMAGE2HTML_NO_BROWSER=1`（不自动开浏览器）和 `IMAGE2HTML_PORT`（指定端口），方便调试。

### 四、验证

- 后端端到端（`e2e.py`）：首页 / 配置读改 / 图床上传并回访直链 / 拒绝非图片 / 历史记录 CRUD，**25 项全通过**；上传确实拿到了 `https://uapis.cn/static/uploads/xxx.webp` 且能 200 回访。
- 前端真实浏览器（headless Edge，同源 iframe 驱动）：
  - 用 `DataTransfer` + `DragEvent` 真实派发 dragenter/dragleave/drop，验证遮罩出现与收起、`isFileDrag` 不会把排序拖拽误判成文件拖拽、上传占位 spinner、上传完成后徽标渲染；
  - 验证 `generate()` 实际提交的 `urls` 是 2 条 http 链接且无空串；
  - 验证排序、删除、空提示词拦截等原有功能无回归。**40 项全通过。**
- 启动链路：重复启动输出「检测到服务已在运行」；5000 被占时输出「端口 5000 被占用，自动改用 5001」。
- 坑：Windows 上不能用 `/c/...` 这类 MSYS 路径当 Chrome/Edge 的参数，必须传 `C:\...`，否则 `--screenshot`/`--user-data-dir` 静默不生效。

## 2026-09-13（第七轮：免 Python 打包 + 降级提示透出）

### 一、背景

用户问「这个软件没有 python 环境可以用吗」。查了一遍：README 里写的「方式三：下载 Release（无需 Python）」**只是文档，仓库里根本没有 exe**，Releases 是空的。属于文档承诺了、但没兑现。

### 二、打包

新增 `build_exe.bat`，双击即完成打包 + 生成分发压缩包：

1. 找 Python（沿用第六轮的思路：**实际执行 `python -c "import sys"` 验证**，而不是只看 `where`）。
2. 检查/安装 PyInstaller。
3. `pyinstaller --onedir --noconfirm --clean --name Image2Html --add-data "templates;templates" app.py`。
4. 生成 `启动.bat`、`使用说明.txt`，并压成 `dist\Image2Html.zip`。

`app.py` 新增冻结态路径处理（原本没有，直接打包会找不到模板）：

```python
BUNDLE_DIR = sys._MEIPASS if getattr(sys, 'frozen', False) else os.path.dirname(os.path.abspath(__file__))
```

分清了两类目录：

- **只读资源**（`templates/`）从 `sys._MEIPASS` 读——PyInstaller 会把它解到包内。
- **用户数据**（`data/` `uploads/` `outputs/`）放 **exe 同级目录**，这样换电脑直接拷文件夹就能带走配置。
  用 `sys.executable` 判断而不是 `sys.argv[0]`：后者在某些启动方式下会是相对路径，`os.path.dirname` 出来是空串，数据就散到当前工作目录了。

技术选择与坑：

- **`--onedir` 而不是 `--onefile`**。onefile 每次启动都要把整个运行时解压到 `%TEMP%`，冷启动明显更慢，杀软也更容易误报。代价是必须整个文件夹一起分发。
- **`--add-data "templates;templates"` 用的是分号**（Windows 上 `源;目标`，Linux/macOS 是 `源:目标`），写错会静默不带进去，运行时才 404。
- 加了 `--exclude-module tkinter/unittest/pydoc/test/distutils/lib2to3` 瘦身。**注意不能排除 `email`**——`http.server` 解析请求头时依赖它。
- 产物 23 MB（zip 后 8.8 MB）。

### 三、中文编码

`build_exe.bat` 里原本用 `echo 中文 > 文件` 生成说明文件，这在 cmd 下会按当前代码页写字节，`chcp 65001` 又和 bat 自身的读取编码打架，很容易乱码。改成：

- `启动.bat` **全用 ASCII**，不出现中文（出错时 `pause` 保留窗口即可，提示语用英文）。
- `使用说明.txt` 交给 PowerShell `Set-Content -Encoding UTF8` 写。
- zip 用 `.NET` 的 `ZipFile.CreateFromDirectory(..., Encoding.UTF8)` 重载，**不能用 `Compress-Archive`**——Windows PowerShell 5.1 的 `Compress-Archive` 不写 UTF-8 标志位，中文文件名解压出来是乱码。验证：`flag_bits & 0x800` 对 `使用说明.txt` / `启动.bat` 均为真。

### 四、降级提示透出

打包后实测上传，发现一个真实缺口：默认图床 UAPI 匿名额度（10 张/天）用完后返回 429，降级链**正确地**换到了 Uguu 并上传成功——但 `handle_upload` 在成功分支把 `errors` 丢掉了，**用户完全不知道自己的图换成了 3 小时就失效的海外图床**。

修法：

- `IMAGE_HOSTS` 给 Uguu 补 `expire` 字段；
- 成功分支：降级过就返回 `warning` + `errors`，未降级但图床本身限时也返回 `warning`；
- 前端 `lastUploadWarning` 接住，`showRefWarning()` 优先展示具体原因，而不是笼统的「备用图床链接可能失效」。

顺带记一个**不是 bug 的现象**：UAPI 限流返回 429 时，`urllib` 拿到的是 `HTTPError`，被 `upload_to_host` 的异常兜底捕获后记进 `errors`、继续试下一个——这正是设计意图，不需要改。

### 五、验证

在**剥离 Python 的 PATH**（只留 `System32`）下，把 zip 解压到全新目录直接跑 exe：

- 启动横幅正常打印，`/api/config`、`/api/history`、首页全部 200；
- 首页字节与源码 `templates/index.html` **md5 完全一致**（`aef5721d…`），确认打包用的是当前代码而不是旧模板；
- exe 同级自动建出 `data/` `uploads/` `outputs/`，路径逻辑正确；
- 上传参考图：成功拿到公网直链，并如实返回 `warning`（UAPI 限流）+ `errors`（UAPI 限流原因、Catbox 断连）。

## 2026-09-13（第八轮：使用教程 PDF + 写教程时挖出的三个真实缺陷）

### 一、需求

「写一个教程 PDF，图片自己截图，要给出**速创充值方法**以及**各个图床使用方法**。」

三件事：① 截图得是真的；② 速创怎么充钱要写清；③ 每个图床怎么申请、怎么用要写清。

### 二、先把事实查实（不靠记忆，全部回到官方文档）

**速创 GPT-Image-2**（[api.wuyinkeji.com/doc/53](https://api.wuyinkeji.com/doc/53)）：

| 项 | 值 |
|----|-----|
| 免费额度 | **无**（这就是「明明填了 Key 还是失败」的根因） |
| 参考价 | 0.1 元/张，扣 10 点/张 |
| 计费优先级 | vip专属 → 单独包月 → 次数包 → 点数计费 → 账户余额 → 免费额度 |
| 注册 | `api.wuyinkeji.com/user/register`（邮箱/手机号 + 6 位验证码 + 密码≥8位含字母数字，邀请码选填） |
| 登录 | `api.wuyinkeji.com/user/login`（微信扫码 或 邮箱密码） |
| **充值** | `api.wuyinkeji.com/user/order`（订单管理） |
| 取 Key | `api.wuyinkeji.com/user/key`（密钥管理） |
| 会员 | `api.wuyinkeji.com/user/vip` |

**各图床**（每一家都去翻了官方文档，不是猜的）：

| 图床 | 申请入口 | 关键细节 |
|------|----------|----------|
| UAPI | `uapis.cn` → 登录拿 Key | 访客 1500 积分；**登录用户 3500 积分/月，每月 1 号重置**；匿名接口每日 10 张 |
| PicUI | `picui.cn` → 个人中心 | API 基址 `https://picui.cn/api/v1`，`POST /upload`，field=`file`，头 `Authorization: Bearer 1\|xxxxx`；**不带 token 也允许匿名上传**；429 = 额度用尽 |
| SM.MS / S.EE | `s.ee` | **前提：账号要有有效套餐**，否则没有 Token；`POST https://s.ee/api/v1/file/upload`，field=`file`（别名 `smfile`），返回 `data.url` |
| ImgURL | `imgurl.org` | 走 SPA 路由 `/user/register`、`/account`、`/upload`；要 **UID + Token，且都在请求体里** |
| ImgBB | `imgbb.com/signup` | 登录后在 API 页拿 `key`，参数 `key` + `image`；海外 |
| Catbox / Uguu / Telegraph | — | 免注册兜底；Uguu 链接约 3 小时失效 |

### 三、写教程顺手挖出来的三个真实缺陷（都已修）

**缺陷 1：SM.MS 图床其实一次都没成功过——`urllib` 的默认 UA 被 Cloudflare 拦了。**

`s.ee` 前面挂了 Cloudflare。`urllib` 默认 UA 是 `Python-urllib/3.x`，被直接判成爬虫：

```
默认 UA  → HTTP 403   body: error code: 1010
浏览器 UA → HTTP 401  body: {"code":-1,"message":"Unauthorized"}
```

403 那个响应体不是 JSON，所以前端只看到一句没头没尾的失败。**token 填对了也一样挂**，
只是没人会同时试两种 UA，所以这个洞一直没被发现。

修法：`app.py` 加 `DEFAULT_UA` 常量，`http_request()` 里**调用方没显式给 User-Agent 就补一个浏览器的**：

```python
DEFAULT_UA = ("Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
              "(KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36")
if not any(k.lower() == 'user-agent' for k in headers):
    headers['User-Agent'] = DEFAULT_UA
```

**缺陷 2：smms 适配器的判断逻辑太脆，且错误信息会退化成没用的 `HTTP 308`。**

两家接口的成功码不一样（sm.ms 是 `code=200`，s.ee 是 `code=0` + `message`），
鉴权写法文档也不一致（sm.ms v2 裸 token / s.ee 写 `Bearer`）。
拿错 token 去试时两种写法返回**完全一样**（都是 401 `Unauthorized`），没法靠返回值区分，所以：
- 4 种「端点 × 鉴权」组合依次试：`s.ee+Bearer` → `s.ee+裸` → `sm.ms+裸` → `sm.ms+Bearer`；
- **成功判据改成「有没有拿到 http 开头的 URL」**，而不是猜状态码；
- 保底错误信息**优先返回非 `HTTP ` 开头的**，否则会被 `sm.ms` 旧域名的裸 `HTTP 308`（跳转）盖掉，
  用户看到的就不再是「Unauthorized（token 不对）」这种真正有用的原因。

**缺陷 3：切英文时，两块 JS 动态生成的内容不翻译。**

`applyLang()` 原来只处理 `[data-i18n]` 标注的静态文案。但参考图的占位卡片和
降级警告条是 `renderRefGrid()` / `showRefWarning()` **动态生成**的，切语言后还是中文。
（这个是在拍英文界面截图时看出来的——截图里赫然一段中文。）

修法：`applyLang()` 末尾补三行重渲染：

```javascript
renderRefGrid();
showRefWarning();
const sel = document.getElementById('hostSelect');
if (sel && hostOptions.length) renderHostOptions(sel.value || 'auto');
```

顺带修了 `AUTO_TIP` 的文案——它原本写的降级顺序和代码里的 `AUTO_ORDER` **对不上**，
等于在设置页里给了用户一份错的地图。现在按实际顺序重写，并说明「需要登录的图床没填 Token 会被直接跳过」。

### 四、截图：真跑起来拍，不是拼图

用 headless Edge 拍**真实运行的应用**（1280×800，`--force-device-scale-factor=1`），共 5 张：
`01-home` 首页 / `02-settings` 设置弹窗 / `03-drag` 拖拽遮罩 / `04-refs` 参考图与降级提示 / `05-en` 英文界面。

拖拽遮罩、参考图徽标这类交互态，靠**同源 iframe** 驱动页内脚本：用 `contentWindow` 拿页面上下文，
派发真实的 `DragEvent` / `DataTransfer`，再用 `eval()` 访问脚本里 `let` 声明的变量
（`refImages`、`pendingUploads` 这些**不会挂到 `window` 上**）。

三个坑（都记进 `docs/tutorial/README.md` 了）：

1. **Edge 的参数必须是 Windows 路径**。在 Git Bash 里传 `/c/...` 不报错，**静默不生效**——
   表现为所有场景拍出来是同一张图（甚至是我那个不存在的 404 页面）。
2. **`msedge.exe` 常常提前返回**（把活交给已有浏览器进程），不能靠进程退出判断完成，**必须轮询等 png 落盘**。
3. **复用同一个 `--user-data-dir` 会继承 `localStorage`**。拍英文图那次，
   上一轮存下的 `lang=en` 让 `toggleLang()` 又翻回了中文，结果 `05-en.png` 和 `01-home.png` 字节完全一样。
   → 每个场景独立 profile，且批量拍摄前先删掉旧的 profile 目录。

那次「两张图一模一样的 bug」排查过程本身也踩了一下：写的调试页把 `out()` 落在 `catch` 里忘了调，
结果只输出两行。改成 `step(label, fn)` 包装函数，无论成败都输出，问题立刻现形。

**第 4 张图里的黄色降级提示不是摆拍**——当时 UAPI 的匿名额度（10 张/天）刚好被这轮测试用光了，
应用**真的**降级到了 Uguu，提示语是它自己生成的：

> 默认图床不可用，已自动换用「Uguu」。该图床链接仅保留约 3 小时，生成前若已过期会抓不到图

### 五、产出

- `Image2Html-使用教程.pdf`（A4，12 页，0.78 MB）：封面 + 目录 + 6 章
  （解压启动 / 速创注册与充值 / 图床申请与使用 / 生成流程 / 排错 / 附录）。
- `docs/tutorial/`：教程模板 `tutorial.tpl.html`、构建脚本 `build_tutorial.py`、
  截图 `shots/`、说明 `README.md`。**不装任何依赖**，有 Edge 就能重出 PDF：
  `python docs/tutorial/build_tutorial.py`。
- 校验：`verify_pdf.py` 检查页数、页面尺寸（209.9×297.0 mm）和 15 项内容要点，全部命中。

### 六、这轮的教训

文档写不下去的地方，往往就是 bug 藏着的地方。
「SM.MS 从来没成功过」和「`AUTO_TIP` 描述的顺序是错的」这两条，
都不是测试报出来的，是**要跟用户解释清楚这个功能怎么用**时才被逼出来的。

